# .env 파일 생성 방법

1. 터미널에서 다음 명령 실행:

```bash
cd /Users/taeheerho/Desktop/oke-front-mcp
echo "FIGMA_TOKEN=여기에_토큰을_붙여넣으세요" > .env
```

2. 또는 직접 생성:
   - 파일명: `.env` (oke-front-mcp 폴더 안에)
   - 내용:
```
FIGMA_TOKEN=figd_여기에_실제_토큰
```

3. 토큰 발급은 `FIGMA_TOKEN_SETUP.md` 참고
